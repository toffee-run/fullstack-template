include:
  - clickhouse/compose.yaml
  - otel-collectors/compose.yaml
  - signoz/compose.yaml
  - traefik/compose.yaml

services:
  traefik:
    ports: [8000:80, 4433:443, 4433:443/udp]

  frontend:
    build: ./frontend

  signoz:
    ports: [8080:8080]
